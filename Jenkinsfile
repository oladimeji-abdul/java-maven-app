pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                echo "building stage..."
            }
        }

        stage("dev") {
            steps {
                echo "dev stage...."
            }
        }
    }
}
