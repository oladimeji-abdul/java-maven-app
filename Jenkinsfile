pipeline {
    agent any
    stages("Build") {
        steps {
            echo "Building app............."
        }
    }

    stages("dev") {
        steps {
            echo 'dev stage init........'
        }
    }
}
